<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Name Letters</title>
</head>

<body>
  <!-- Write your code here -->
  
  <% if (locals.numberofLetters) { %>   
    <h1>Number of letters in your name are <%= numberofLetters %></h1>
  <% } else { %>
    <h1>Write your name below :</h1>
  <% } %>
  <!-- here we are using "numberofLetters" because in "index.js we have have saved the value of "numLetters" in "numberofLetters" -->
   
    <!-- locals property is an object that contains response local variables scoped to the request and because of this, it is only available to the view(s) rendered during that request/response cycle (if any). -->

  <form action="/submit" method="POST">
    <input type="text" name="fName" placeholder="First name">
    <input type="text" name="lName" placeholder="Last name">
    <input type="submit" value="OK">
  </form>
</body>

</html>
